# Scripts

One-off and infrequently-run operational helpers — VCR cassette refresh, Langfuse prompt/dataset sync, registry validation, ad-hoc data wrangling.

Each script is invokable via `uv run python scripts/<name>.py`. Where useful, scripts are also wired to `make` targets.
