# strata_forge.cli

Typer-based CLI surface. Subcommands: `chat`, `eval`, `experiments`, `prompts`, `datasets`, `train`, `serve`, `compute`, and `doctor` (environment + reachability diagnostic). The skeleton + `doctor` land first; each remaining subcommand fills in alongside its underlying module.

> Implementation pending. See `docs/roadmap.md` for current status.
