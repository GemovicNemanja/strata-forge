# strata_forge.training

Fine-tuning primitives: SFT via TRL `SFTTrainer`; preference tuning (DPO, ORPO, KTO, GRPO); PEFT (LoRA, QLoRA); chat-template formatting; sequence packing; and trainer entry-point scripts shipped to SkyPilot / SSH for remote execution. Langfuse tracing for run metadata.

> Implementation pending. See `docs/roadmap.md` for current status.
