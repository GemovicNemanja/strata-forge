# forge.prompts

Jinja2 prompt templating with safe filters and a Langfuse-backed prompt registry (versioning, sync, few-shot example injection from Langfuse datasets). Templates explicitly model the stable prefix vs dynamic suffix split so provider prompt caching just works.

> Implementation pending. See `docs/roadmap.md` for current status.
