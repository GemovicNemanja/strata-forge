# forge.tracing

Langfuse observability: auto-tracing of LLM calls via the LiteLLM callback, `@traced` decorator and span context managers for non-LLM code paths, structlog integration with `trace_id` correlation, and custom metric/score helpers.

> Implementation pending. See `docs/roadmap.md` for current status.
