# forge.datasets

Dataset CRUD + versioning, a bridge between Langfuse Datasets and Hugging Face Datasets, and synthetic data generation primitives (self-instruct, distillation). Feeds into the experiment runner in `forge.evals`.

> Implementation pending. See `docs/roadmap.md` for current status.
