# Recipes

How-to guides for common workflows that cross module boundaries — e.g. "run a model evaluation with a custom grader," "deploy a fine-tuned model with vLLM on SkyPilot," "build a RAG pipeline against a Qdrant collection." Each recipe is a short, opinionated walkthrough that references concrete code paths.
