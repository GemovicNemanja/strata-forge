# Module guides

One Markdown file per `strata_forge.*` module, named after the module (e.g. `core.md`, `config.md`, `llm.md`). Each file is the canonical reference for that module's public API, internal patterns, and usage recipes. Linked from the root `CLAUDE.md`.
